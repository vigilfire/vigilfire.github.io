App screenshots shown on the marketing site (`../index.html`).

These are **generated**, not live captures: standalone HTML mock-ups that reuse
the Vigil Fire app's own CSS with realistic sample data, rendered to PNG with
headless Chrome at 2× for retina displays.

| File | Section | What it shows |
|---|---|---|
| `site-register.png` | Hero | A site's equipment register — service type, status accents, printable register / certificate |
| `due-list.png` | "The Due list is the day's work" | Cross-site Due & overdue dashboard, most urgent first |
| `equipment-form.png` | "No signal in the basement?" | The combined equipment + inspection form |
| `trainee-logbook.png` | "The SAQCC trainee logbook" | A trainee's view — training status, daily logbook, competency progress |

When the live app (`https://vigilfire.github.io/app/`) changes, refresh the
matching mock-up and re-render.
